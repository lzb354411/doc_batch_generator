# 文档批量生成工具 · 开发文档

> 更新日期：2026-09-19

## 一、项目概述

本项目把原有的 Python 脚本 [doc_batch_generator.py](file:///e:/workgo/work/doc_batch_generator/doc_batch_generator.py)（面向 AI 平台的"热拔插脚本"）改造成一个**桌面 GUI 应用程序（EXE）**，供普通用户在 Windows 上直接使用。

**核心功能**：借助一张 Excel 数据表和一套文档模板，按规则批量生成文件资料。

- 从本地 Excel 读取数据（识别标题行、Sheet 列表）
- 用「内容规则」（标题列 = 内容值）或「数字规则」（第 x 行到第 y 行）选中数据行
- 每条规则单独配置模板（支持 .xlsx / .xlsm / .docx / .csv / .txt）
- 把模板中的【列名】占位符按数据行内容替换；【列名|日期格式】支持日期格式化；未匹配的占位符原样保留
- 按命名项叠加生成文件名，可自动建立子文件夹；重名自动加 ` (2)` 序号
- 生成完成后自动打开输出文件夹

## 二、技术栈

| 组件 | 选型 | 版本 | 用途 |
|------|------|------|------|
| 语言 | Go | 1.27 | 编译为单文件 EXE |
| GUI 框架 | Fyne | v2.8.1 | 桌面界面 |
| Excel 读写 | excelize | v2.11.0 | 读取数据源、替换 xlsx/xlsm 模板 |
| 文本编码 | golang.org/x/text | v0.42.0 | GBK 编码的 txt/csv 模板解码 |
| docx 处理 | 无第三方库 | — | 直接对 docx 内部 XML 做字节级替换（见 5.4） |

Python 版依赖 openpyxl / python-docx，Go 版不再依赖 Python 运行时。

## 三、项目结构

```
e:\workgo\work\doc_batch_generator\
├── doc_batch_generator.py      # 原 Python 脚本（金标准，作为对比基准）
├── templates\                  # 用户真实模板（20 个 .xlsx 工程文档模板）
└── docgen\                     # Go 工程根目录
    ├── go.mod / go.sum         # Go 模块依赖
    ├── main.go                 # 入口：GUI 启动 / CLI 模式（-config）
    ├── run.json                # CLI 验证用测试配置
    ├── build.ps1               # 正式构建脚本（图标资源 + 编译 + 校验）
    ├── install.iss             # Inno Setup 安装包脚本
    ├── winres\winres.json      # go-winres 资源定义（图标/清单/版本信息）
    ├── assets\                 # 应用图标：icon.png / icon.ico / setup_icon.ico / icon.svg
    ├── internal\
    │   ├── core\               # 核心引擎（与 UI 无关，可单独测试）
    │   │   ├── generator.go    # 主流程 Run()、配置结构、配置存取
    │   │   ├── datasource.go   # Excel 读取、日期样式判定、Sheet 列表
    │   │   ├── cellvalue.go    # 单元格值类型与字符串/日期格式化
    │   │   ├── placeholder.go  # 占位符替换与提取
    │   │   ├── rules.go        # 规则选行、模板解析
    │   │   ├── naming.go       # 文件名/文件夹名构建、重名避让
    │   │   ├── xlsxgen.go      # xlsx/xlsm/csv/txt 模板生成
    │   │   ├── docxgen.go      # docx 占位符替换（字节级）
    │   │   └── *_test.go       # 单元测试（9 个文件、41 个用例）
    │   └── ui\                 # Fyne 图形界面（每页一个文件）
    │       ├── app.go          # 窗口、导航框架、配置持久化、版本号
    │       ├── theme.go        # 中文字体加载（simhei.ttf）
    │       ├── datasource_panel.go   # 1 数据源页
    │       ├── rules_panel.go        # 2 数据行规则页
    │       ├── templates_panel.go    # 3 模板配置页
    │       ├── naming_panel.go       # 4 文件命名页
    │       ├── output_panel.go       # 5 输出设置页
    │       └── run_panel.go          # 执行页
    ├── cmd\
    │   ├── mkfixtures\main.go  # 生成测试夹具（测试模板/数据）
    │   ├── mkreal\main.go      # 真实模板验收（生成数据/配置 + -verify 校验）
    │   ├── mkicon\main.go      # 代码绘制应用图标（SVG/PNG/ICO）
    │   └── pygolden\           # 金标准对比验证
    │       ├── driver.py       # 用 Python 版 run() 跑同一份夹具
    │       └── compare.py      # 对比 Python 版与 Go 版输出
    ├── testdata\               # 测试数据与三方输出目录
    │   ├── data.xlsx           # 测试数据表（4 个项目）
    │   ├── templates\          # 测试模板（docx/xlsx/csv/txt/GBK txt）
    │   ├── real\               # 真实模板验收夹具（data.xlsx/config.json/out）
    │   ├── out\                # Go CLI 版输出
    │   ├── out_gui\            # Go GUI 版输出
    │   └── out_python\         # Python 版输出（金标准）
    └── dist\资料文档生成安装包-v1.0.0.exe   # 安装包产物
```

## 四、架构设计

### 4.1 核心原则：core 与 ui 分离

- `internal/core` 是纯逻辑引擎，不依赖任何 GUI 代码，可独立运行和测试。
- `internal/ui` 只负责界面交互，所有生成逻辑都调用 core。
- **`core.RunConfig` 是唯一数据源**：GUI 所有控件的改动即时写回内存中的 `RunConfig`；点击「保存配置」或关闭窗口时持久化到 EXE 同目录的 `config.json`，下次启动自动恢复。

### 4.2 回调机制（core → UI 的通信）

```go
type RunCallbacks struct {
    Log       func(line string)     // 实时日志
    Progress  func(done, total int) // 进度（total 规划完成后固定）
    Cancelled func() bool           // 返回 true 时在下一个文件前中止
}
```

core.Run 不关心调用方是谁——GUI 传入回调实现实时日志、进度条与取消；CLI 模式只传一个打印日志的回调。

### 4.3 线程模型（GUI 执行页）

- `core.Run` 在独立 goroutine 中执行，避免阻塞界面。
- 所有 UI 更新通过 `fyne.Do` 切回主线程。
- 取消采用 `atomic.Bool` 标志：core.Run 在每个文件开始前检查，完成当前文件后即中止。
- 日志区为每行一个 Label 的 VBox（不用 List），支持不同高度的自动换行日志行，追加后自动滚动到底部；并发访问由 `logMu` 保护。

### 4.4 与 Python 版的语义对齐

每个 core 函数注释中标注了与 Python 版函数的对应关系（如 `SelectContentRows` 对应 `_select_content_rows`）。关键语义：

- **行号 1 基**：标题行 = 第 N 行、数字规则"第 x 行至第 y 行"与 Excel 中看到的一致（空白行同样占一行）。
- **数据行范围**：从标题行的下一行扫描到末尾，跳过空行；读取后截断尾部连续空行。
- **命名规则**：替换非法字符 → 去首尾空白 → 去尾部句点 → 折叠连续空白；模板显示名去掉「序号前缀」和「模板-」前缀（`1.模板-开工报告.xlsx` → `开工报告`）；文件名超 255 字符（按 Unicode 字符数）则报错。
- **重名避让**：输出路径已存在时自动追加 ` (2)`、` (3)` …（与 Python 版一致）。

## 五、核心引擎详解（internal/core）

### 5.1 主流程 generator.go

`Run(cfg RunConfig, cb *RunCallbacks) RunResult` 的五个阶段：

1. **参数校验**：Excel 存在、标题行 > 0、输出目录存在、Sheet 已选、至少一条启用的规则、至少一个命名项。
2. **加载数据**：`LoadSheetRows` 读取整表 → `TruncateTrailingEmptyRows` 截断尾部空行 → `BuildHeaderMap` 构建 {列名: 列索引}（重复列名取首个）。
3. **规划**：内容规则先于数字规则（均按列表顺序）执行。每条规则解析模板 → 展开为「数据行 × 模板」任务列表；某规则未匹配到数据行或模板缺失时记为一条失败明细，不影响其他规则。
4. **执行**：逐任务生成。按命名项构建文件夹名（若开启子文件夹）→ 构建文件名 → 解决重名 → `generateFile` 按扩展名分发生成器。每个文件开始前检查取消标志。
5. **汇总**：返回成功/失败计数、明细列表与汇总文字。

配置结构（可序列化为 JSON）：

```go
type RunConfig struct {
    ExcelPath         string              // 数据源 Excel
    SheetName         string              // 数据 Sheet
    HeaderRow         int                 // 标题行（1 基）
    OutputFolder      string              // 输出目录
    CreateSubfolder   bool                // 是否按命名项建子文件夹
    ContentRules      []ContentRule       // 内容规则
    NumberRules       []NumberRule        // 数字规则
    NamingItems       []NamingItem        // 命名项（顺序叠加）
    Groups            map[string][]string // 模板分组（预留）
    DefaultTemplateDir string             // 默认模板目录
}
```

### 5.2 数据读取 datasource.go

- 用 excelize 打开 Excel，遍历工作表行；`readCell` 保留类型信息：文本 → 字符串、数值 → 浮点（整数化显示）、日期格式单元格 → 时间值、公式 → 缓存结果。
- **日期判定**：Excel 日期本质是"数值 + 日期数字格式"。自行解析 `xl/styles.xml` 的 cellXfs 与 numFmt 格式码，用与 openpyxl `is_date_format` 一致的启发式判断，并支持 1900/1904 日期系统。
- **纯时间单元格**（数值 0~1 且日期格式）渲染为 `HH:mm:ss`。

### 5.3 单元格值与格式化 cellvalue.go

- `CellValue` 用 `Str` / `Num` / `T` 三字段区分文本、数值、日期时间。
- `String()`：文本去首尾空白；整数浮点 → 整数形式；日期时间 → `YYYY-MM-DD HH:mm:ss`。
- `applyFormat` 支持占位符中的日期格式令牌：`YYYY/YY/MM/DD/HH/mm/ss`（可组合字面字符），无法解析为日期时返回原字符串。日期解析支持 9 种输入格式（含 `2006年01月02日` 等）。

### 5.4 占位符替换 placeholder.go 与 docxgen.go

**占位符语法**：`【列名】`、`【列名|日期格式】`。正则 `【([^【】]+)】`，未匹配的占位符**原样保留**并收集进 unmatched 列表。

**xlsx/xlsm**：用 excelize 逐工作表、逐单元格查找含 `【` 的文本并替换，其余内容（样式、公式、其他单元格）保持原样。

**docx**：不引入第三方库，直接对 docx 内部的 `word/document.xml` 做**字节级修改**：
- 以段落 `w:p` 为单位，把段内全部文本 run（`w:t`）合并后做占位符替换，结果写回首个 `w:t`、清空其余 `w:t`（保留首 run 的字符格式）——与 python-docx 语义一致。
- 支持 XML 实体转义/反转义（`&amp;`、`&#数字;` 等）、引号感知的标签解析、段落嵌套（如文本框内段落）。
- 重写 zip 时只替换 `document.xml` 一个条目，其余部件按原始压缩数据原样复制，保证未被修改的内容逐字节不变。
- 表格单元格内的段落同样处理。

**csv/txt**：先按 UTF-8 读取，无法解码则按 GBK；换行统一 `\r\n`（CSV 的 CRLF 符合 RFC 4180）。

**生成前校验**：`TemplatePlaceholders` 提取模板中全部占位符名（与替换遍历方式一致），供执行页与 Excel 标题列比对，提前发现缺失列。

## 六、GUI 详解（internal/ui）

### 6.1 布局

单窗口（默认 1080×720）+ 左侧步骤导航 + 右侧内容区 + 底部状态栏。六个步骤：

1. **数据源**：选择 Excel 文件、数据 Sheet、标题所在行，带数据预览表格；加载后把行数据与标题列缓存到 App 供其他页读取。
2. **数据行规则**：内容规则 / 数字规则的增删、启用禁用、上移下移；实时显示每条规则命中行数（`→ 命中 X 行`）。
3. **模板配置**：为每条规则选择模板文件夹与模板，支持「按勾选模板」（从文件夹扫描结果中多选）与「按模板分组」（勾选分组）两种模式，另附全局「管理分组」对话框（新建/重命名/删除/编辑成员）。
4. **文件命名**：命名项（列值 / 模板名）的增删与排序，实时显示文件名示例。
5. **输出设置**：输出文件夹、是否按命名项新建子文件夹。
6. **执行**：生成前占位符校验（发现问题弹窗确认）→ 开始生成 → 进度条 + 实时日志 + 取消 → 结果摘要 → 自动打开输出文件夹。

### 6.2 配置持久化

- 控件改动即时写回内存 `cfg`。
- 点击底部「保存配置」或关闭窗口时静默保存到 `config.json`（EXE 同目录；`go run` 的临时构建目录除外，退回当前工作目录）。
- 启动时自动恢复上次配置；上次的 Excel 不存在时给出提示。

### 6.3 Windows 特性处理

- **中文字体**：Fyne 内置字体不含中文，启动时从 `C:\Windows\Fonts\simhei.ttf` 加载并包装默认主题，否则中文显示为方框。
- **DPI 感知**：`main.go` 中声明进程 DPI 感知（按系统能力从新到旧尝试），解决 Windows 125% 缩放下文字模糊、界面偏大的问题。

### 6.4 调试钩子（环境变量）

| 变量 | 作用 |
|------|------|
| `DOCGEN_CONFIG` | 指定 config.json 路径 |
| `DOCGEN_START_PAGE` | 启动后直接跳到指定页面（0-5） |
| `DOCGEN_AUTOSTART=1` | 窗口显示后自动开始生成（自动化冒烟测试） |
| `DOCGEN_DEBUG=1` | 在执行页日志输出画布尺寸与缩放（DPI 排查） |

## 七、开发进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | 方案设计：Go + Fyne、core/ui 分层、上下移按钮替代拖拽排序 | ✅ 已完成 |
| 1 | 环境准备（Go、GCC、Fyne CLI） | ✅ 已完成 |
| 1 | 核心引擎移植（internal/core 全部函数） | ✅ 已完成 |
| 1 | 金标准验证（与 Python 版输出逐字节/逐文件对比） | ✅ 已完成 |
| 2 | GUI 框架：窗口、导航、状态栏 | ✅ 已完成 |
| 3 | 5 个配置页 + 中文字体 + DPI + config.json 持久化 | ✅ 已完成 |
| 4 | 执行页：进度条、实时日志、取消、自动打开文件夹、生成前占位符校验 | ✅ 已完成 |
| 4 | 测试版 EXE（docgen-gui-test.exe，无终端窗口） | ✅ 已完成 |
| 5 | 模板分组管理 UI（core 已支持 group 模式，界面未做） | ✅ 已完成 |
| 5 | Go 单元测试（internal/core 共 41 个用例，`go test ./internal/core/` 通过） | ✅ 已完成 |
| 5 | 正式发布：产品名「资料文档生成」、代码绘制图标、版本号 1.0.0、Inno Setup 安装包 | ✅ 已完成 |
| 5 | 用真实模板目录（templates/ 20 个模板）做最终验收（40 文件全成功） | ✅ 已完成 |

## 八、验证与测试

### 8.1 金标准对比（核心引擎）

测试夹具：`docgen/testdata/` 下的 `data.xlsx`（4 个项目）+ 5 个覆盖不同格式的模板（docx / xlsx / csv / txt / GBK 编码 txt），覆盖占位符、日期格式化、子文件夹、重名避让等场景。

对比流程（在 `docgen/` 目录）：

```bash
# 1. Python 版生成金标准输出（写入 testdata/out_python）
python ..\docgen\cmd\pygolden\driver.py

# 2. Go CLI 版生成输出（写入 testdata/out）
go run . -config run.json

# 3. 对比两个输出目录
python ..\docgen\cmd\pygolden\compare.py
```

说明：docx 文件两者内容一致，但文件体积不同属正常——Go 版做字节级 zip 重写，Python 版（python-docx）会整体重新打包，压缩率不同。

### 8.2 Go 单元测试（核心引擎）

`internal/core/` 下共 9 个 `*_test.go`、41 个用例，覆盖命名、选行、日期格式化、占位符替换、docx/xlsx/txt 模板生成与主流程 `Run`（成功 / 校验失败 / 取消 / 配置存取）。集成用例直接复用 8.1 的金标准夹具（`testdata/`），**不需要安装 Python 环境**。

```bash
cd docgen
go test ./internal/core/ -count=1      # 运行全部用例
go test ./internal/core/ -count=1 -v   # 详细输出
```

说明：先确保 `testdata/` 夹具已生成（`go run ./cmd/mkfixtures`），否则相关用例会报「缺少测试夹具」。

### 8.3 GUI 冒烟测试

利用 `DOCGEN_AUTOSTART=1` + `DOCGEN_CONFIG` 指向测试配置，启动后自动执行一次完整生成，检查 `testdata/out_gui` 输出与 CLI 版一致。

### 8.4 人工验收

双击 EXE 走完六个页面配置 → 执行生成，重点检查：中文显示、DPI 缩放、日志实时性、进度条、取消按钮、完成自动打开文件夹、关窗自动保存配置。

### 8.5 真实模板验收（正式版）

用用户真实业务模板目录（`templates/`，20 个工程文档表格模板）做端到端验收。`cmd/mkreal` 负责生成验收夹具与校验：

```bash
cd docgen
go run ./cmd/mkreal                            # 生成 testdata/real/data.xlsx 与 config.json（覆盖 11 个占位符列，2 个项目）
go run . -config testdata/real/config.json     # 执行一次性批量生成（20 模板 × 2 项目 = 40 个文件）
go run ./cmd/mkreal -verify                    # 校验：2 个子文件夹各 20 文件 + 抽查开工报告日期/工作票勾选/乡镇列
```

验收结果（2026-09-19）：40 个文件全部生成成功，抽查内容替换正确（项目名称、开工/竣工时间、所属线路、安全措施 √、乡镇列），重名避让生效（两个同名「甲供物资超欠供分析表」模板自动加 ` (2)` 序号）。

## 九、构建与运行

环境要求：Go 1.27+；Windows 需安装 MSYS2 mingw64 GCC（Fyne 依赖 CGO）。

> **重要（CGO 构建前提）**：MSYS2 的 gcc 需要其 `bin` 目录在 PATH 中才能找到 cc1/as/ld 等子进程，否则 gcc 静默失败、报 `cgo: exit status 2`。构建脚本 `build.ps1` 已处理，手动编译时须先执行：
> ```powershell
> $env:PATH = "C:\msys64\mingw64\bin;" + $env:PATH
> $env:CGO_ENABLED = "1"
> ```

```bash
cd docgen

# 开发调试运行（GUI）
go run .

# 命令行模式（验证核心引擎用）
go run . -config run.json

# 一键正式构建（推荐）：图标资源 + 版本号注入 + 编译 + 校验产物
powershell -ExecutionPolicy Bypass -File build.ps1

# 编译安装包（依赖 build.ps1 产出的「资料文档生成.exe」）
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" install.iss
```

生成物：
- 正式 EXE：`docgen/资料文档生成.exe`（约 26MB，无终端窗口，内嵌图标与版本信息）。
- 安装包：`docgen/dist/资料文档生成安装包-v1.0.0.exe`（约 11MB，安装到 Program Files、创建开始菜单/桌面快捷方式）。
- 运行后 `config.json` 自动生成在 EXE 同目录。

### 9.1 正式发布的四项配置

| 项 | 说明 | 位置 |
|----|------|------|
| 产品名 | 窗口标题「资料文档生成 v1.0.0」 | `internal/ui/app.go` 的 `Version` 变量 + `app.NewWindow` |
| 图标 | 纯代码绘制（4x 超采样抗锯齿），SVG 为设计源 | `cmd/mkicon/main.go` → `assets/` |
| 版本号 | `-ldflags "-X docgen/internal/ui.Version=1.0.0"` 注入 | `build.ps1` |
| EXE 资源 | 图标/清单/版本信息（go-winres 生成 `.syso` 由 go build 自动链接） | `winres/winres.json` + `go-winres make` |

注意：安装器图标（`assets/setup_icon.ico`）必须用经典 BMP 帧，Inno Setup 不接受纯 PNG 帧的 ICO；应用 EXE 图标（`assets/icon.ico`）用 PNG 帧即可。

## 十、配置说明（config.json）

示例（`docgen/testdata/config.gui.json`）：

```json
{
  "excel_path": "testdata/data.xlsx",
  "sheet_name": "Sheet1",
  "header_row": 3,
  "output_folder": "testdata/out_gui",
  "create_subfolder": true,
  "content_rules": [
    {
      "enabled": true,
      "title_column": "类型",
      "content_value": "开工",
      "template_folder": "testdata/templates",
      "template_mode": "all",
      "selected_templates": ["1.模板-开工报告.docx"]
    }
  ],
  "number_rules": [],
  "naming_items": [
    { "type": "column", "key": "项目名称" },
    { "type": "template", "key": "" }
  ],
  "groups": {}
}
```

字段说明见 `RunConfig` 结构体（5.1）。`template_mode` 为 `"all"`（按勾选模板）或 `"group"`（按分组）。

## 十一、已知限制与注意事项

- 数据源仅支持**本地 Excel**。Python 版的「中控台数据表格」「金山文档在线表格」两种数据源面向 AI 平台运行环境，桌面版不需要，未移植。
- 模板分组（`Groups`）是全局的，各规则在模板页选择「按模板分组」模式后勾选要使用的分组。
- 不支持 `.xls`（旧版二进制格式），需先在 Excel 中另存为 .xlsx/.xlsm。
- docx 替换保留首 run 的字符格式，跨 run 的混合格式段落（如占位符拆在多个 run 且格式不同）会统一为首 run 格式——与 Python 版行为一致。
- 文件名长度上限 255 字符（按 Unicode 字符数），超长该文件失败并在日志中报原因。

## 十二、后续开发建议

1. **体验优化**：执行时禁止/提示切换页面；日志按文件分条高亮失败项；进度统计按规则分组展示。
2. **安装包完善**：升级检测与自动更新；针对公司环境做静默安装/组策略分发。
